---
title: "Authenticity Feelings"
format: html
---

```{r, include=FALSE}
# Initialize dashboardr page configuration (CSS/JS for charts)
dashboardr:::.page_config()
```

```{r setup}
#| echo: false
#| warning: false
#| message: false
#| error: false
#| results: 'hide'

# Load dashboardr (includes dplyr, highcharter as dependencies)
library(dashboardr)

# Global chunk options
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  fig.width = 12,
  fig.height = 8,
  dpi = 300
)

# Load data
data <- readRDS('dataset_large_34805obs.rds')

# Data summary
cat('Dataset loaded:', nrow(data), 'rows,', ncol(data), 'columns\n')

# Create filtered datasets
# Each filter is applied once and reused across visualizations

data_filtered_03566455 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Netherlands")
data_filtered_beb8a137 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Denmark")
data_filtered_a1803a37 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "USA")
data_filtered_6dcfcc28 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Brazil")
data_filtered_4bc4c9f5 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "South Africa")
data_filtered_0c062950 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Japan")

```


We measured this by asking ...

## Background Variables


::: {.panel-tabset}

### Authenticity Feelings


::: {.panel-tabset}

##### Overall

```{r background-variables-authenticity-feelings}
# All countries
result <- viz_boxplot(
  data = data,
  title = "All countries",
  y_var = "value",
  x_var = "item",
  horizontal = TRUE,
  x_label = "",
  y_label = "Feeling thermometer (0 = negative, 100 = positive)",
  tooltip_suffix = " / 100"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Netherlands

```{r background-variables-authenticity-feelings-2}
# Netherlands
result <- viz_boxplot(
  data = data_filtered_03566455,
  title = "Netherlands",
  y_var = "value",
  x_var = "item",
  horizontal = TRUE,
  x_label = "",
  y_label = "Feeling thermometer (0 = negative, 100 = positive)",
  tooltip_suffix = " / 100"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Denmark

```{r background-variables-authenticity-feelings-3}
# Denmark
result <- viz_boxplot(
  data = data_filtered_beb8a137,
  title = "Denmark",
  y_var = "value",
  x_var = "item",
  horizontal = TRUE,
  x_label = "",
  y_label = "Feeling thermometer (0 = negative, 100 = positive)",
  tooltip_suffix = " / 100"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### USA

```{r background-variables-authenticity-feelings-4}
# USA
result <- viz_boxplot(
  data = data_filtered_a1803a37,
  title = "USA",
  y_var = "value",
  x_var = "item",
  horizontal = TRUE,
  x_label = "",
  y_label = "Feeling thermometer (0 = negative, 100 = positive)",
  tooltip_suffix = " / 100"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Brazil

```{r background-variables-authenticity-feelings-5}
# Brazil
result <- viz_boxplot(
  data = data_filtered_6dcfcc28,
  title = "Brazil",
  y_var = "value",
  x_var = "item",
  horizontal = TRUE,
  x_label = "",
  y_label = "Feeling thermometer (0 = negative, 100 = positive)",
  tooltip_suffix = " / 100"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### South Africa

```{r background-variables-authenticity-feelings-6}
# South Africa
result <- viz_boxplot(
  data = data_filtered_4bc4c9f5,
  title = "South Africa",
  y_var = "value",
  x_var = "item",
  horizontal = TRUE,
  x_label = "",
  y_label = "Feeling thermometer (0 = negative, 100 = positive)",
  tooltip_suffix = " / 100"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Japan

```{r background-variables-authenticity-feelings-7}
# Japan
result <- viz_boxplot(
  data = data_filtered_0c062950,
  title = "Japan",
  y_var = "value",
  x_var = "item",
  horizontal = TRUE,
  x_label = "",
  y_label = "Feeling thermometer (0 = negative, 100 = positive)",
  tooltip_suffix = " / 100"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


:::


:::

