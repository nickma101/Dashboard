---
title: "AI Journalism Attitudes"
format: html
---

```{r, include=FALSE}
# Initialize dashboardr page configuration (CSS/JS for charts)
dashboardr:::.page_config()
```

```{r setup}
#| echo: false
#| warning: false
#| message: false
#| error: false
#| results: 'hide'

# Load dashboardr (includes dplyr, highcharter as dependencies)
library(dashboardr)

# Global chunk options
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  fig.width = 12,
  fig.height = 8,
  dpi = 300
)

# Load data
data <- readRDS('dataset_large_6961obs.rds')

# Data summary
cat('Dataset loaded:', nrow(data), 'rows,', ncol(data), 'columns\n')

# Create filtered datasets
# Each filter is applied once and reused across visualizations

data_filtered_03566455 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Netherlands")
data_filtered_beb8a137 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Denmark")
data_filtered_a1803a37 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "USA")
data_filtered_6dcfcc28 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Brazil")
data_filtered_4bc4c9f5 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "South Africa")
data_filtered_0c062950 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Japan")

```

## AI Journalism Attitudes


::: {.panel-tabset}

### Willingness to engage


::: {.panel-tabset}

##### Overall

```{r ai-journalism-attitudes-willingness-to-engage}
# AI News Engagement Intentions
result <- viz_stackedbar(
  data = data,
  title = "AI News Engagement Intentions",
  x_vars = c("Q6_read", "Q6_share", "Q6_trust", "Q6_talk", "Q6_ignore"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Netherlands

```{r ai-journalism-attitudes-willingness-to-engage-2}
# AI News Engagement Intentions
result <- viz_stackedbar(
  data = data_filtered_03566455,
  title = "AI News Engagement Intentions",
  x_vars = c("Q6_read", "Q6_share", "Q6_trust", "Q6_talk", "Q6_ignore"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Denmark

```{r ai-journalism-attitudes-willingness-to-engage-3}
# AI News Engagement Intentions
result <- viz_stackedbar(
  data = data_filtered_beb8a137,
  title = "AI News Engagement Intentions",
  x_vars = c("Q6_read", "Q6_share", "Q6_trust", "Q6_talk", "Q6_ignore"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### USA

```{r ai-journalism-attitudes-willingness-to-engage-4}
# AI News Engagement Intentions
result <- viz_stackedbar(
  data = data_filtered_a1803a37,
  title = "AI News Engagement Intentions",
  x_vars = c("Q6_read", "Q6_share", "Q6_trust", "Q6_talk", "Q6_ignore"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Brazil

```{r ai-journalism-attitudes-willingness-to-engage-5}
# AI News Engagement Intentions
result <- viz_stackedbar(
  data = data_filtered_6dcfcc28,
  title = "AI News Engagement Intentions",
  x_vars = c("Q6_read", "Q6_share", "Q6_trust", "Q6_talk", "Q6_ignore"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### South Africa

```{r ai-journalism-attitudes-willingness-to-engage-6}
# AI News Engagement Intentions
result <- viz_stackedbar(
  data = data_filtered_4bc4c9f5,
  title = "AI News Engagement Intentions",
  x_vars = c("Q6_read", "Q6_share", "Q6_trust", "Q6_talk", "Q6_ignore"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Japan

```{r ai-journalism-attitudes-willingness-to-engage-7}
# AI News Engagement Intentions
result <- viz_stackedbar(
  data = data_filtered_0c062950,
  title = "AI News Engagement Intentions",
  x_vars = c("Q6_read", "Q6_share", "Q6_trust", "Q6_talk", "Q6_ignore"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


:::


### Hyperpersonalisation


::: {.panel-tabset}

##### Overall

```{r ai-journalism-attitudes-hyperpersonalisation}
# Attitudes towards hyperpersonalised news
result <- viz_stackedbar(
  data = data,
  title = "Attitudes towards hyperpersonalised news",
  x_vars = c("Q9_engaging", "Q9_improving_user_experience", "Q9_missing_important_news", "Q9_risking_privacy", "Q9_spreading_misinfo"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Netherlands

```{r ai-journalism-attitudes-hyperpersonalisation-2}
# Attitudes towards hyperpersonalised news
result <- viz_stackedbar(
  data = data_filtered_03566455,
  title = "Attitudes towards hyperpersonalised news",
  x_vars = c("Q9_engaging", "Q9_improving_user_experience", "Q9_missing_important_news", "Q9_risking_privacy", "Q9_spreading_misinfo"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Denmark

```{r ai-journalism-attitudes-hyperpersonalisation-3}
# Attitudes towards hyperpersonalised news
result <- viz_stackedbar(
  data = data_filtered_beb8a137,
  title = "Attitudes towards hyperpersonalised news",
  x_vars = c("Q9_engaging", "Q9_improving_user_experience", "Q9_missing_important_news", "Q9_risking_privacy", "Q9_spreading_misinfo"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### USA

```{r ai-journalism-attitudes-hyperpersonalisation-4}
# Attitudes towards hyperpersonalised news
result <- viz_stackedbar(
  data = data_filtered_a1803a37,
  title = "Attitudes towards hyperpersonalised news",
  x_vars = c("Q9_engaging", "Q9_improving_user_experience", "Q9_missing_important_news", "Q9_risking_privacy", "Q9_spreading_misinfo"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Brazil

```{r ai-journalism-attitudes-hyperpersonalisation-5}
# Attitudes towards hyperpersonalised news
result <- viz_stackedbar(
  data = data_filtered_6dcfcc28,
  title = "Attitudes towards hyperpersonalised news",
  x_vars = c("Q9_engaging", "Q9_improving_user_experience", "Q9_missing_important_news", "Q9_risking_privacy", "Q9_spreading_misinfo"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### South Africa

```{r ai-journalism-attitudes-hyperpersonalisation-6}
# Attitudes towards hyperpersonalised news
result <- viz_stackedbar(
  data = data_filtered_4bc4c9f5,
  title = "Attitudes towards hyperpersonalised news",
  x_vars = c("Q9_engaging", "Q9_improving_user_experience", "Q9_missing_important_news", "Q9_risking_privacy", "Q9_spreading_misinfo"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Japan

```{r ai-journalism-attitudes-hyperpersonalisation-7}
# Attitudes towards hyperpersonalised news
result <- viz_stackedbar(
  data = data_filtered_0c062950,
  title = "Attitudes towards hyperpersonalised news",
  x_vars = c("Q9_engaging", "Q9_improving_user_experience", "Q9_missing_important_news", "Q9_risking_privacy", "Q9_spreading_misinfo"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


:::


### Aspects influencing credibility judgements


::: {.panel-tabset}

##### Overall

```{r ai-journalism-attitudes-aspects-influencing-credib}
# Aspects influencing credibility judgements
result <- viz_stackedbar(
  data = data,
  title = "Aspects influencing credibility judgements",
  x_vars = c("Q8_trust_publisher", "Q8_realism", "Q8_misinfo_risk", "Q8_human_in_the_loop"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Netherlands

```{r ai-journalism-attitudes-aspects-influencing-credib-2}
# Aspects influencing credibility judgements
result <- viz_stackedbar(
  data = data_filtered_03566455,
  title = "Aspects influencing credibility judgements",
  x_vars = c("Q8_trust_publisher", "Q8_realism", "Q8_misinfo_risk", "Q8_human_in_the_loop"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Denmark

```{r ai-journalism-attitudes-aspects-influencing-credib-3}
# Aspects influencing credibility judgements
result <- viz_stackedbar(
  data = data_filtered_beb8a137,
  title = "Aspects influencing credibility judgements",
  x_vars = c("Q8_trust_publisher", "Q8_realism", "Q8_misinfo_risk", "Q8_human_in_the_loop"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### USA

```{r ai-journalism-attitudes-aspects-influencing-credib-4}
# Aspects influencing credibility judgements
result <- viz_stackedbar(
  data = data_filtered_a1803a37,
  title = "Aspects influencing credibility judgements",
  x_vars = c("Q8_trust_publisher", "Q8_realism", "Q8_misinfo_risk", "Q8_human_in_the_loop"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Brazil

```{r ai-journalism-attitudes-aspects-influencing-credib-5}
# Aspects influencing credibility judgements
result <- viz_stackedbar(
  data = data_filtered_6dcfcc28,
  title = "Aspects influencing credibility judgements",
  x_vars = c("Q8_trust_publisher", "Q8_realism", "Q8_misinfo_risk", "Q8_human_in_the_loop"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### South Africa

```{r ai-journalism-attitudes-aspects-influencing-credib-6}
# Aspects influencing credibility judgements
result <- viz_stackedbar(
  data = data_filtered_4bc4c9f5,
  title = "Aspects influencing credibility judgements",
  x_vars = c("Q8_trust_publisher", "Q8_realism", "Q8_misinfo_risk", "Q8_human_in_the_loop"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


##### Japan

```{r ai-journalism-attitudes-aspects-influencing-credib-7}
# Aspects influencing credibility judgements
result <- viz_stackedbar(
  data = data_filtered_0c062950,
  title = "Aspects influencing credibility judgements",
  x_vars = c("Q8_trust_publisher", "Q8_realism", "Q8_misinfo_risk", "Q8_human_in_the_loop"),
  stacked_type = "percent",
  x_label = "",
  y_label = "Percent (%)",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


:::


:::

