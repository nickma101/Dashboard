---
title: "Regulatory Preferences"
format: html
---

```{r, include=FALSE}
# Initialize dashboardr page configuration (CSS/JS for charts)
dashboardr:::.page_config()
```

```{r setup}
#| echo: false
#| warning: false
#| message: false
#| error: false
#| results: 'hide'

# Load dashboardr (includes dplyr, highcharter as dependencies)
library(dashboardr)

# Global chunk options
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  fig.width = 12,
  fig.height = 8,
  dpi = 300
)

# Load data
data <- readRDS('dataset_large_48727obs.rds')

# Data summary
cat('Dataset loaded:', nrow(data), 'rows,', ncol(data), 'columns\n')

# Create filtered datasets
# Each filter is applied once and reused across visualizations

data_filtered_03566455 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Netherlands")
data_filtered_beb8a137 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Denmark")
data_filtered_a1803a37 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "USA")
data_filtered_6dcfcc28 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Brazil")
data_filtered_4bc4c9f5 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "South Africa")
data_filtered_0c062950 <- dashboardr:::.convert_haven(data) %>% dplyr::filter(country == "Japan")

```


We measured this by asking ...

## Q11 mentions


::: {.panel-tabset}

### Netherlands

```{r q11-mentions}
# Q11 aspects — Netherlands
result <- viz_bar(
  data = data_filtered_03566455,
  title = "Q11 aspects — Netherlands",
  x_var = "item",
  value_var = "mentioned",
  bar_type = "mean",
  x_order = c("Human rights", "State & national security", "Economic wellbeing", "Relationships with others", "Environment", "Technological innovation", "Religious beliefs / local traditions"),
  color_palette = c("#4E79A7", "#F28E2B", "#E15759", "#76B7B2", "#59A14F", "#EDC948", "#B07AA1"),
  x_label = "",
  y_label = "% mentioned",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


### Denmark

```{r q11-mentions-2}
# Q11 aspects — Denmark
result <- viz_bar(
  data = data_filtered_beb8a137,
  title = "Q11 aspects — Denmark",
  x_var = "item",
  value_var = "mentioned",
  bar_type = "mean",
  x_order = c("Human rights", "State & national security", "Economic wellbeing", "Relationships with others", "Environment", "Technological innovation", "Religious beliefs / local traditions"),
  color_palette = c("#4E79A7", "#F28E2B", "#E15759", "#76B7B2", "#59A14F", "#EDC948", "#B07AA1"),
  x_label = "",
  y_label = "% mentioned",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


### USA

```{r q11-mentions-3}
# Q11 aspects — USA
result <- viz_bar(
  data = data_filtered_a1803a37,
  title = "Q11 aspects — USA",
  x_var = "item",
  value_var = "mentioned",
  bar_type = "mean",
  x_order = c("Human rights", "Economic wellbeing", "State & national security", "Technological innovation", "Environment", "Relationships with others", "Religious beliefs / local traditions"),
  color_palette = c("#76B7B2", "#4E79A7", "#B07AA1", "#F28E2B", "#EDC948", "#59A14F", "#E15759"),
  x_label = "",
  y_label = "% mentioned",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


### Brazil

```{r q11-mentions-4}
# Q11 aspects — Brazil
result <- viz_bar(
  data = data_filtered_6dcfcc28,
  title = "Q11 aspects — Brazil",
  x_var = "item",
  value_var = "mentioned",
  bar_type = "mean",
  x_order = c("Human rights", "Economic wellbeing", "Environment", "Technological innovation", "State & national security", "Religious beliefs / local traditions", "Relationships with others"),
  color_palette = c("#76B7B2", "#4E79A7", "#EDC948", "#F28E2B", "#B07AA1", "#E15759", "#59A14F"),
  x_label = "",
  y_label = "% mentioned",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


### South Africa

```{r q11-mentions-5}
# Q11 aspects — South Africa
result <- viz_bar(
  data = data_filtered_4bc4c9f5,
  title = "Q11 aspects — South Africa",
  x_var = "item",
  value_var = "mentioned",
  bar_type = "mean",
  x_order = c("Human rights", "Economic wellbeing", "Environment", "Technological innovation", "State & national security", "Religious beliefs / local traditions", "Relationships with others"),
  color_palette = c("#76B7B2", "#4E79A7", "#EDC948", "#F28E2B", "#B07AA1", "#E15759", "#59A14F"),
  x_label = "",
  y_label = "% mentioned",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


### Japan

```{r q11-mentions-6}
# Q11 aspects — Japan
result <- viz_bar(
  data = data_filtered_0c062950,
  title = "Q11 aspects — Japan",
  x_var = "item",
  value_var = "mentioned",
  bar_type = "mean",
  x_order = c("Human rights", "Economic wellbeing", "State & national security", "Technological innovation", "Environment", "Relationships with others", "Religious beliefs / local traditions"),
  color_palette = c("#76B7B2", "#4E79A7", "#B07AA1", "#F28E2B", "#EDC948", "#59A14F", "#E15759"),
  x_label = "",
  y_label = "% mentioned",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


### Overall

```{r q11-mentions-7}
# Q11 aspects — all countries
result <- viz_bar(
  data = data,
  title = "Q11 aspects — all countries",
  x_var = "item",
  value_var = "mentioned",
  bar_type = "mean",
  x_order = c("Human rights", "Economic wellbeing", "State & national security", "Technological innovation", "Environment", "Relationships with others", "Religious beliefs / local traditions"),
  color_palette = c("#4E79A7", "#F28E2B", "#E15759", "#76B7B2", "#59A14F", "#EDC948", "#B07AA1"),
  x_label = "",
  y_label = "% mentioned",
  tooltip_suffix = "%"
)

# Extract cross-tab data BEFORE wrapping (wrapping strips attributes)
cross_tab_data <- attr(result, 'cross_tab_data')
cross_tab_config <- attr(result, 'cross_tab_config')
cross_tab_id <- attr(result, 'cross_tab_id')

# Embed cross-tab data for client-side filtering
if (!is.null(cross_tab_data)) {
  cross_tab_json <- jsonlite::toJSON(cross_tab_data, dataframe = 'rows')
  config_json <- jsonlite::toJSON(cross_tab_config, auto_unbox = TRUE)
  script_tag <- htmltools::tags$script(
    htmltools::HTML(paste0(
      'window.dashboardrCrossTab = window.dashboardrCrossTab || {};',
      'window.dashboardrCrossTab["', cross_tab_id, '"] = {',
      'data: ', cross_tab_json, ',',
      'config: ', config_json,
      '};'
    ))
  )
  result <- htmltools::tagList(script_tag, result)
}

result
```


:::

